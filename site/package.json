{
  "name": "sp-treeview-site",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "dev": "npm run bundle && astro dev",
    "build": "npm run bundle && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.4.0",
    "@astrojs/starlight": "^0.34.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
